<a href="./"class="btn btn-dark">seznam zaměstnanců</a>
<a href="../logout" class="btn btn-warning">Odhlásit</a>
<form method="post">
    {{#employee}}
    <div class="mt-3">
    <label for="name">Name</label>
        <input type="text" name="name" id="name" value="{{employee.name}}" class="form-control"/>
        {{#errors.name}}<small class="text-danger">{{errors.name}}</small>{{/errors.name}}
    </div>

    <div class="mt-3">
    <label for="surname">Surname</label>
        <input type="text" name="surname" id="surname" value="{{employee.surname}}" class="form-control"/>
        {{#errors.surname}}<small class="text-danger">{{errors.surname}}</small>{{/errors.surname}}
    </div>

    <div class="mt-3">
    <label for="job">Job</label>
        <input type="text" name="job" id="job" value="{{employee.job}}" class="form-control"/>
        {{#errors.job}}<small class="text-danger">{{errors.job}}</small>{{/errors.job}}
    </div>

        <div class="mt-3">
            <label for="wage">Wage</label>
            <input type="text" name="wage" id="wage" value="{{wage}}" class="form-control"/>
            {{#errors.wage}}<small class="text-danger">{{errors.wage}}</small>{{/errors.wage}}
        </div>

        <div class="mt-3">
            <label for="room">Room</label>
<!--            <input type="text" name="room" id="room" value="{{room}}" class="form-control"/>-->

            <select name="room" id="room">
                {{#mistnost}}
                    <option value="{{room_id}}">{{name}}</option>
                {{/mistnost}}
            </select>
            {{#errors.room}}<small class="text-danger">{{errors.room}}</small>{{/errors.room}}
        </div>

    <div class="mt-3">
    {{#room.room_id}}<input type="hidden" name="room_id" value="{{room.room_id}}"/>{{/room.room_id}}
        <input type="hidden" name="action" value="{{action}}" />
        <input type="submit" value="{{action}}" class="btn btn-primary" />
    </div>
        <input type="hidden" name="employee_id" value="{{employee_id}}" />

</form>
<form method="post" action="changePassword.php">
    <div class="mt-3">
        <label for="password">Password: </label>
        <input type="password" name="password" id="password" value="" />
        <input type="hidden" value="{{employee_id}}" name="id" class="btn btn-primary" />
        <input type="submit" value="změnit heslo" class="btn btn-primary" />
    </div>
</form>
    {{/employee}}
<a href="./"class="btn btn-dark">seznam zaměstnanců</a>
<a href="../logout.php" class="btn btn-warning">Odhlásit</a>

<form method="post">
    {{#room}}
    <div class="mt-3">
    <label for="name">Name</label>
        <input type="text" name="name" id="name" value="{{room.name}}" class="form-control"/>
        {{#errors.name}}<small class="text-danger">{{errors.name}}</small>{{/errors.name}}
    </div>

    <div class="mt-3">
    <label for="no">No.</label>
        <input type="text" name="no" id="no" value="{{no}}" class="form-control"/>
        {{#errors.No}}<small class="text-danger">{{errors.No}}</small>{{/errors.No}}
    </div>

    <div class="mt-3">
    <label for="phone">Phone</label>
        <input type="text" name="phone" id="phone" value="{{phone}}" class="form-control"/>
        {{#errors.phone}}<small class="text-danger">{{errors.phone}}</small>{{/errors.phone}}
    </div>

    <div class="mt-3">
    {{#room.room_id}}<input type="hidden" name="room_id" value="{{room.room_id}}"/>{{/room.room_id}}
        <input type="hidden" name="action" value="{{action}}" />
        <input type="submit" value="{{action}}" class="btn btn-primary" />
    </div>
    {{/room}}
</form>